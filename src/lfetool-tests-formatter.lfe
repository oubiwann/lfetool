(defmodule lfetool-tests-formatter
  (export all))

(defun display-failures (state)
  'noop)

(defun display-pending (state)
  'noop)

(defun display-profile (state)
  'noop)

(defun display-timing (state)
  'noop)

(defun display-results (data state)
  'noop)

